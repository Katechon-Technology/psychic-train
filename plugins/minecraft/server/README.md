This is the Minecraft Fabric-loader server directory. Small text configs
(`server.properties`, `eula.txt`, `ops.json`, `whitelist.json`, etc.) live here and
are committed. Everything else is gitignored and must be populated locally.

See `../README.md` for setup instructions.
